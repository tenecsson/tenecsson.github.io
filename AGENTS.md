When writing markdown, always use $$ for mathjax. If you find \( or \) replace them with $$. Single $ does not work. Display math is done by using line breaks like this (without the codefence)
```markdown
Text

$$
E=mc^2
$$

More text
```

If you find yourself referencing another post in this site, use markdown link syntax
